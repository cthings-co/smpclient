# SMPWirepasTransport examples

Each of these can be run from the root of the repository:
```
python -m examples.wirepas.<EXAMPLE> <address>
```
e.g.
```
python -m examples.wirepas.helloworld <address>
```

All examples need Wirepas node address as an argument.
